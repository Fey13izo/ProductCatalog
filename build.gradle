// Top-level build file where you can add configuration options common to all sub-projects/modules.

// DO NOT ADD PLUGINS HERE. They are managed in settings.gradle
// The 'buildscript' block has been removed as it is legacy.

// You can define a clean task if you wish, but it's often not necessary.
task clean(type: Delete) {
    delete rootProject.buildDir
}
